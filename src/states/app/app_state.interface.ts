import { MenuOptionsEnum } from '../../widgets/models/menu-options-enum';

/**
 * State variables that the app will need to keep track of
 */
export interface AppState {
    currMenuOption: MenuOptionsEnum;
}